services:
  postgres:
    build:
      context: .
      dockerfile: db.Dockerfile
    container_name: db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d postgres"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks:
      - app-network

  auth_service:
    build:
      context: ../
      dockerfile: docker/auth_service.Dockerfile
    container_name: auth_service
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ../services/auth_service/app:/app/app
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:8000/v1/api/health', timeout=2).status == 200 else 1)"]
      interval: 10s
      timeout: 3s
      retries: 15
      start_period: 15s
    restart: unless-stopped
    networks:
      - app-network
  
  rate_service:
    build:
      context: ../
      dockerfile: docker/rate_service.Dockerfile
    container_name: rate_service
    ports:
      - "8001:8001"
    depends_on:
      postgres:
        condition: service_healthy
      auth_service:
        condition: service_healthy
    volumes:
      - ../services/rate_service/app:/app/app
    restart: unless-stopped
    networks:
      - app-network

volumes:
  postgres_data:
  redis_data:

networks:
  app-network:
    driver: bridge
